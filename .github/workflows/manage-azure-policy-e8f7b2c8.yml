name: manage-azure-policy-e8f7b2c8
on:
  workflow_dispatch:
    inputs:
      remarks:
        description: 'Reason for triggering the workflow run'
        required: false
        default: 'Updating Azure Policies'


## Uncomment the following if you wish to automatically trigger workflow run with every commit
#
#  push:
#    paths:
#      - 'PAC_ARM_Template_Libarary_development/PolicyDefinitions/policies/Accenture_-_Network_interfaces_should_disable_IP_forwarding_4118ec4c-34cf-4740-8f1b-8dbe9a0e5c3b/**'
#      - .github/workflows/manage-azure-policy-e8f7b2c8.yml
jobs:
  apply-azure-policy:
    runs-on: ubuntu-latest
    steps:
    - name: Checkout
      uses: actions/checkout@v2
    - name: Login to Azure
      uses: azure/login@v1
      with:
        creds: ${{secrets.AZURE_CREDENTIALS_e8f7b2c8}}
        allow-no-subscriptions: true
    - name: Create or Update Azure Policies
      uses: azure/manage-azure-policy@v0
      with:
        paths: |
          PAC_ARM_Template_Libarary_development/PolicyDefinitions/policies/Accenture_-_Network_interfaces_should_disable_IP_forwarding_4118ec4c-34cf-4740-8f1b-8dbe9a0e5c3b/**
